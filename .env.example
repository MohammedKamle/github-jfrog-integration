# JFrog Artifactory configuration for local development
# Copy to .env and fill in your values

# Artifactory URL (e.g. https://yourorg.jfrog.io for SaaS)
JF_URL=https://mdk96.jfrog.io

# Authentication (use one of the two options)

# Option 1: Access token (preferred)
JF_ACCESS_TOKEN=your-jfrog-access-token

# Option 2: Username + API key (fallback)
# JF_USER=your-username
# JF_API_KEY=your-api-key

# Repository names (must match your Artifactory setup)
JF_NPM_VIRTUAL=demo-npm
JF_NPM_LOCAL=demo-npm-local

# Optional: Enable Xray scan in workflows
# JF_XRAY_SCAN_ENABLED=true
